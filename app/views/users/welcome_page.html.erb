<h1> WELCOME BACK <%= @user.full_name %> </h1>

<a href="/blogs">Go make a blog post!</a>

<!-- <form action="blogs/create" method="post">
  <input type="text" name="blog[title]" placeholder="Title">
  <input type="text" name="blog[message]" placeholder="Message">
  <input type="submit" value="blog">

</form> -->
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Message</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @blogs.each do |blog| %>
      <tr>
        <td><%= blog.title unless blog.user_id != @user.id %></td></a>
        <td><%= blog.message unless blog.user_id != @user.id %></td>
        <td><%= blog.user.full_name unless blog.user_id != @user.id %></td>
        <td><%= link_to 'Show', blog unless blog.user_id != @user.id %></td>
      </tr>
    <% end %>
  </tbody>
</table>
